{
  "name": "socks-proxy",
  "version": "1.0.0",
  "description": "socks代理",
  "main": "index.js",
  "scripts": {
    "test": "node ./demo.js",
    "prod": "export NODE_ENV=prod && pm2 start ./demo.js --name=${PWD##*/}",
    "prod-reload": "export NODE_ENV=prod && pm2 reload ${PWD##*/}"
  },
  "author": "wleonardo.github.io",
  "license": "ISC"
}
